---
# Used only for Debian/Ubuntu (Debian OS-Family)
# https://docs.docker.com/engine/install/debian/#uninstall-old-versions

docker_obsolete_packages:
  - docker
  - docker.io
  - docker-engine
  - docker-doc
  - docker-compose
  - docker-compose-v2
  - podman-docker
  - containerd
  - runc

docker_rootless_prerequisite_packages:
  - uidmap
  - dbus-user-session

docker_rootless_capability_packages:
  - libcap2-bin

docker_rootless_network_packages:
  slirp4netns:
    - slirp4netns
  pasta:
    - passt
  gvisor-tap-vsock: []
